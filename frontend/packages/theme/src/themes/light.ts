import { ThemeInterface } from '../index';

const colors = {
  primaryBg: '#2c6157',
  secondaryBg: '#6fd056',
  primaryText: '#ffffff',
  secondaryText: '#ffffff',
};

const theme: ThemeInterface = {
  type: 'light',
  colors: colors,
};

export default theme;
